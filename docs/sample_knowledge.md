# Sample Knowledge

LangChain and MCP-style tools are easier to inspect when the repository keeps the retrieval corpus small and focused.

This demo keeps its sample knowledge in markdown files so the retrieval path is obvious and testable.
