# Configuration

Required:

- `OPENAI_API_KEY`

Optional:

- `OPENAI_MODEL`: defaults to `gpt-4o-mini`
- `MCP_HOST`: defaults to `127.0.0.1`
- `MCP_PORT`: defaults to `8000`
- `MCP_TRANSPORT`: defaults to `stdio`

The project reads the same OpenAI key naming convention used in earlier projects.
